# DEBUG RENDERER – list all names seen

{%- for result in results %}
  {%- set node = result.ymli_parent %}
  {%- set depth = 0 %}
  {%- while node %}
Name at depth {{ depth }}: {{ node.name }}
  {%- set node = node.ymli_parent if node.ymli_parent else None %}
  {%- set depth = depth + 1 %}
  {%- endwhile %}
{%- endfor %}
